<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Editar Produto</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #1a0000;
            color: #fff;
        }
        h2, h3.well {
            color: #ff4d4d;
        }
        .badge {
            background-color: #800000;
            color: #fff;
        }
        .card {
            background-color: #2a0000;
            border: 1px solid #990033;
        }
        .card-header {
            background-color: #1a0000;
            border-bottom: 1px solid #990033;
        }
        label {
            color: #ff4d4d;
        }
        .form-control {
            background-color: #3a0000;
            color: #fff;
            border: 1px solid #990033;
        }
        .form-control:focus {
            border-color: #cc0033;
            box-shadow: 0 0 0 0.2rem rgba(204, 0, 51, 0.25);
        }
        .btn-danger {
            background-color: #cc0033;
            border: none;
            color: #fff;
        }
        .btn-danger:hover {
            background-color: #800000;
        }
        .btn-primary {
            background-color: #990033;
            border: none;
            color: #fff;
        }
        .btn-primary:hover {
            background-color: #800020;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="container my-4">
        <div class="jumbotron">
            <!-- Cabeçalho da página -->
            <div class="row">
                <h2>NODE - CRUD <span class="badge">v 2.0.0</span></h2>
            </div>
        </div>

        <div class="span10 offset1">
            <div class="card">
                <div class="card-header">
                    <h3 class="well">Atualizar produto</h3>
                </div>
                <form class="form-horizontal" action="/edit/<%= Produto.id %>" method="POST" enctype="multipart/form-data">
                    <div class="card-body">
                        <label class="control-label" for="name">Nome:</label>
                        <input class="form-control" type="text" name="name" id="name" value="<%= Produto.name %>" required><br>

                        <label class="control-label" for="descricao">Descrição:</label>
                        <input class="form-control" type="descricao" name="descricao" id="descricao" value="<%= Produto.descricao %>" required><br>

                        <label class="control-label" for="preco">Preço:</label>
                        <input class="form-control" type="preco" name="preco" id="preco" value="<%= Produto.preco %>" required><br>

                    </div>
                    <div class="control-group">
                        <label for="categoria">Categoria:</label>
                        <select class="form-control" name="categoria" id="categoria" required>
                            <option value="monitores" <%= Produto.categoria === 'monitores' ? 'selected' : '' %>>Monitores</option>
                            <option value="gabinetes" <%= Produto.categoria === 'gabinetes' ? 'selected' : '' %>>Gabinetes</option>
                            <option value="acessorios" <%= Produto.categoria === 'acessorios' ? 'selected' : '' %>>Acessórios</option>
                        </select>
                    </div>
                    <div class="control-group">
                        <label for="imagem">Imagem do Produto:</label>
                        <input class="form-control" type="file" name="imagem" id="imagem" accept="image/*" />
                        <input type="hidden" name="imagemAntiga" value="<%= Produto.imagem %>">
                        <% if (Produto.imagem) { %>
                            <img src="<%= Produto.imagem %>" alt="Imagem atual" height="100">
                        <% } %>
                    </div>
                    
                    <div class="form-actions d-flex gap-2">
                        <button type="submit" class="btn btn-danger">Alterar</button>
                        <a href="/" class="btn btn-primary">Voltar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
